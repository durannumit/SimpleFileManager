<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ThreadExampleActivity" >

    <TextView
        android:id="@+id/externalPathTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:textSize="18sp" />

    <Button
        android:id="@+id/setDefaultPathButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/externalPathTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="41dp"
        android:onClick="buttonClick"
        android:text="@string/button_text" />

    <EditText
        android:id="@+id/defaultPathE1ditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/externalPathTextView"
        android:layout_alignBottom="@+id/externalPathTextView"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/externalPathTextView"
        android:ems="10"
        android:inputType="textPersonName" />

</RelativeLayout>